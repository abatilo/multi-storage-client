<!-- To avoid merge conflicts, add items at an arbitrary place in the list. -->

## Breaking Changes

## New Features

- Add `--include` and `--exclude` pattern filtering to `msc ls` command.
- Enhance `SyncResult` to separately track files and bytes added/deleted for complete sync operation visibility.

## Bug Fixes

- Fix automatic retry for HTTP 408 (Request Timeout) errors when accessing Google Cloud Storage via S3-compatible API.
